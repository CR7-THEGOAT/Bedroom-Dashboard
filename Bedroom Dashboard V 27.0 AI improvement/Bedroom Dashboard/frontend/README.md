# Bedroom Dashboard Frontend

The current Vite/React frontend is kept at the project root for compatibility with the existing working app:

- `src/`
- `public/`
- `package.json`
- `vite.config.mjs`

This folder marks the frontend part of the Bedroom Dashboard architecture without moving files and breaking the existing dev server.
